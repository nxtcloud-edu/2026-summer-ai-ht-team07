# TIMELINE.md — 24시간 마일스톤

> **이 일정의 단 하나의 규칙**
> **M3(전체 시간의 절반 지점)에서 파이프라인이 조악하게라도 end-to-end로 돌아야 한다.**
> 나머지 절반은 전부 개선과 안정화다.
>
> 해커톤에서 팀이 망하는 전형적인 패턴은 "각자 자기 파트를 완벽하게 만들다가
> 마지막 3시간에 처음 합쳐보는 것"이다. 그때 안 붙으면 복구할 시간이 없다.
> 먼저 붙이고 나중에 잘 만든다. 순서를 바꾸지 않는다.

**기준 시각**: 2026-08-10(월) 13:00 시작 가정 — 실제 시작 시각에 맞춰 오프셋만 조정할 것.

---

## 전체 개요

| | 마일스톤 | 경과 | 시각(가정) | 이 시점에 반드시 돌아가야 하는 것 |
|---|---|---|---|---|
| **M0** | 계약 확정 | 0 → 1h | 13:00 → 14:00 | `schema.py` 확정, 전원 `make setup` 성공 |
| **M1** | 데이터 확보 | 1 → 4h | 14:00 → 17:00 | `make data` 가 검수 통과 데이터를 만든다 |
| **M2** | 모델 확보 | 4 → 8h | 17:00 → 21:00 | `make train` 이 비교표와 모델을 만든다 |
| **M3** | **🔴 End-to-End** | 8 → 12h | 21:00 → 01:00 | **`make demo` 로 4개 탭이 전부 뜬다** |
| **M4** | 품질 개선 | 12 → 18h | 01:00 → 07:00 | 지표·설명·가이드가 발표 가능한 수준 |
| **M5** | 발표 자료 | 18 → 21h | 07:00 → 10:00 | 슬라이드 완성, 숫자가 산출물과 일치 |
| **M6** | 안정화·리허설 | 21 → 24h | 10:00 → 13:00 | **기능 추가 금지.** 리허설 3회 |

---

## M0 — 계약 확정 (0~1h) 🔒

**목표**: 그 뒤 11시간 동안 서로 안 물어봐도 되게 만든다.

| 담당 | 할 일 |
|---|---|
| 전원 | 리포지토리 clone, `make setup`, `make test` 통과 확인 |
| E | `src/yeda/schema.py` 리뷰 → 확정 선언. 이후 변경은 E를 통해서만 |
| A + B | 13개 변수의 단조 제약 방향 8개를 확정 (특히 진공압 **부호 규약**) |
| 전원 | `ROLES.md` 의 A~E 자리에 실제 이름 기입 |
| 전원 | 브랜치 생성: `feat/data-C`, `feat/model-C`, `feat/explain-D`, `feat/ui-E`, `docs/physics-A`, `docs/arch-B` |

**완료 판정**
- [ ] 5명 전원의 노트북에서 `make test` 가 통과한다 (51개 테스트).
- [ ] `schema.py` 에 "확정" 커밋이 있고 전원이 pull 했다.
- [ ] 각자 자기 소유 파일 목록을 말할 수 있다.

> ⚠️ **여기서 30분을 아끼려다 나중에 3시간을 잃는다.** 계약 확정은 서두르지 않는다.

---

## M1 — 데이터 확보 (1~4h)

**목표**: 방어 가능한 합성 데이터가 손에 있다.

| 담당 | 할 일 |
|---|---|
| A | `configs/data_gen.yaml` 의 가중치를 현직자 면담 근거로 조정, `PHYSICS_RATIONALE.md` 초안 |
| C | `make data` 실행 → 검수 리포트 확인 → A에게 전달, 필요 시 `latent.scale` 재보정 |
| D | (대기 없음) `tests/test_optimize.py` 의 `fake_score` 로 최적화 뼈대 작성 |
| E | Streamlit 레이아웃 + mock 백엔드로 4개 탭 껍데기 완성 |
| B | `ARCHITECTURE.md` 초안 — 센서 취득 경로 |

**이 시점에 반드시 돌아가야 하는 것**
- ✅ `make data` → `data/raw/yeda_synthetic.csv` 생성, **경고 0건**
- ✅ 성공률 65~80%, 베이즈 정확도 0.82~0.90
- ✅ `pytest tests/test_generator.py` 전부 통과
- ✅ `make demo` 가 **mock 모드로** 뜬다 (모델 없이)

**중단 기준**: 3시간이 지나도 생성기 검수가 통과 못 하면, 현재 파라미터를 그대로 동결하고 M2로 넘어간다.
데이터 튜닝은 M4에서 다시 할 수 있지만, 모델이 없으면 M3가 불가능하다.

---

## M2 — 모델 확보 (4~8h)

**목표**: 3종 비교표와 저장된 본선 모델.

| 담당 | 할 일 |
|---|---|
| C | `make train` → 4종 비교, 신뢰도 곡선, `primary_model.joblib` 저장 |
| C | 단조 제약 유무 성능 비교 → **제약이 더 나쁘면 즉시 A와 방향 재검토** |
| D | 실제 `ModelBundle` 로 SHAP 전환, 기여도 합 = 예측값 검증 |
| E | `backend.py` 실물 연결 테스트, 데모 프리셋 3개 값 조정 |
| A | 변수별 기여 곡선 검수, `PHYSICS_RATIONALE.md` 완성 |
| B | `QA_DEFENSE.md` 질문 15개 작성 |

**이 시점에 반드시 돌아가야 하는 것**
- ✅ `artifacts/metrics/model_comparison.csv` 에 4행이 있다
- ✅ 홀드아웃 정확도 80% 초중반 (95%+ 면 즉시 생성기 재점검)
- ✅ `artifacts/models/primary_model.joblib` 로드 가능
- ✅ SHAP 이 확률 %p 단위로 나오고 합이 맞는다

**중단 기준**: LightGBM이 안 되면 RandomForest로 간다. SHAP TreeExplainer는 둘 다 지원한다.

---

## M3 — 🔴 End-to-End (8~12h) — **가장 중요한 마일스톤**

**목표**: 조악해도 좋다. **끝에서 끝까지 한 번 돈다.**

| 담당 | 할 일 |
|---|---|
| E | mock 제거하고 실물 백엔드로 4개 탭 전부 연결 |
| D | `recommend()` 를 UI에 연결, 개선 가이드 문장 출력 |
| E | 알림 탭 dry-run 본문 표시까지 연결 |
| 전원 | **다 같이 모여서 데모를 한 번 돌린다.** 각자 자기 화면에서 확인 |
| A + B | 데모를 보며 "실무자가 이걸 보고 행동할 수 있는가" 관점으로 지적 |

**이 시점에 반드시 돌아가야 하는 것 — 이것이 M3의 전부다**

```
make demo
  → 프리셋 "위험 조건" 선택
  → ① 예측 성공률이 숫자로 뜬다
  → ② SHAP 워터폴이 그려진다
  → ③ "무엇을 얼마로 바꾸면 몇 %p" 문장이 나온다
  → ④ 알림 메일 본문이 화면에 표시된다
```

- [ ] 위 4단계가 **한 번의 실행에서 전부** 된다
- [ ] mock 배지가 사라져 있다 (실물 모델 사용 중)
- [ ] 팀원 5명 모두 각자 노트북에서 재현했다

> 🚨 **M3를 12시간 지점까지 통과하지 못하면 즉시 범위를 줄인다.**
> 우선 버릴 것 순서: ④ 알림 → SECOM → ③ 최적화 → ② 설명.
> ①만 남아도 데모는 성립한다. **아무것도 안 도는 것보다 훨씬 낫다.**

---

## M4 — 품질 개선 (12~18h)

**목표**: 발표에서 인용 가능한 수준으로 올린다. 여기서부터는 **있는 걸 좋게 만드는 시간**이다.

| 담당 | 할 일 |
|---|---|
| C | 하이퍼파라미터 조정, 임계값 재조정(PR 곡선), 신뢰도 곡선 시각화 |
| D | SHAP 워터폴 디자인 개선, 최적화 제약 튜닝(비현실적 제안 제거) |
| E | UI 문구 정리, 한계 고지 문구 배치, 예외 처리 보강 |
| A | 데이터 카드(`docs/DATA_CARD.md`) 작성 |
| B | SECOM 착수 **(메인 데모가 완전할 때만)**, `QA_DEFENSE.md` 20개로 확장 |

**이 시점에 반드시 돌아가야 하는 것**
- ✅ 모델 비교표의 숫자를 발표에서 그대로 인용할 수 있다
- ✅ 최적화 제안이 물리적으로 납득 가능하다 (A가 승인)
- ✅ SHAP 한계 고지가 UI에 보인다
- ✅ 합성 데이터 명시가 UI와 README에 있다

> 💡 **새 기능을 추가하고 싶어지는 구간이다. 참는다.**
> M4에서 추가한 기능은 M6에서 버그로 돌아온다.

---

## M5 — 발표 자료 (18~21h)

**목표**: 슬라이드의 모든 숫자가 `artifacts/` 의 산출물과 일치한다.

| 담당 | 할 일 |
|---|---|
| 전원 | 슬라이드 작성 (아래 구성 참고) |
| C | 표의 숫자를 `model_comparison.csv` 에서 복사 — **기억으로 쓰지 않는다** |
| A | 물리 근거 슬라이드, 단조 제약 서사 |
| B | 아키텍처 슬라이드, SECOM 보조 슬라이드 1장 |
| E | 데모 시연 순서 대본 작성 |

**슬라이드 구성 (권장)**
1. 문제 — 후공정 다이 픽업, 왜 중소 OSAT인가 (트랙 1 연결)
2. 접근 — 예측 / 원인 분해 / 최적화 / 알림
3. **데이터 — 합성 데이터임을 명시** + 설계 방법론
4. 모델 — 3종 비교표 + **단조 제약 서사(핵심 차별점)**
5. 성능 — PR-AUC, recall, 신뢰도 곡선
6. 데모 (라이브)
7. 한계와 향후 계획 — **먼저 말한다**
8. (보조) SECOM 일반화 검증

**이 시점에 반드시 돌아가야 하는 것**
- ✅ 슬라이드의 모든 수치에 출처 파일이 있다
- ✅ "합성 데이터" 슬라이드가 3번째 안에 있다 (숨기지 않는다)

---

## M6 — 안정화 · 리허설 (21~24h) 🔒

**목표**: 발표가 사고 없이 끝난다.

> 🚫 **이 구간에서 기능 추가·리팩터링 전면 금지.**
> 담당: E가 이 규칙을 강제한다. "5분이면 되는데"가 데모를 죽인다.

| 시각 | 할 일 |
|---|---|
| 21~22h | **전체 리허설 1회** — 발표 + 데모 전 과정. 시간 측정 |
| 22~23h | 발견된 버그 중 **데모를 멈추는 것만** 수정. 나머지는 방치 |
| 23~23.5h | **리허설 2회** + B 주도 Q&A 모의 (`QA_DEFENSE.md` 기반) |
| 23.5~24h | **리허설 3회**, 백업 준비, 노트북 전원·네트워크 확인 |

**백업 체크리스트 (E)**
- [ ] `make demo` 가 **오프라인**에서도 뜬다 (네트워크 의존 없음 확인)
- [ ] 데모 화면 **녹화본**이 있다 — 라이브가 실패하면 즉시 전환
- [ ] `artifacts/metrics/model_comparison.csv` 가 커밋되어 있다 (재학습 실패 대비)
- [ ] 발표용 노트북에서 처음부터 끝까지 1회 성공했다
- [ ] alerts `dry_run` 설정을 확인했다 (실제 발송 시연할지 최종 결정)

**최종 완료 판정**
- [ ] 리허설 3회 중 최소 2회가 사고 없이 끝났다
- [ ] 발표 시간이 제한 시간 안에 들어온다
- [ ] Q&A 예상 질문 20개에 답변자가 지정되어 있다

---

## 시간이 부족할 때 버리는 순서

앞에 있을수록 먼저 버린다. **①은 절대 버리지 않는다.**

1. ~~SECOM 일반화 검증~~ (보조 슬라이드일 뿐)
2. ~~이메일 실제 발송~~ (dry-run 본문 표시로 충분)
3. ~~Optuna 최적화~~ (좌표 하강으로 충분)
4. ~~교차검증~~ (`--fast` 로 홀드아웃만)
5. ~~RandomForest 비교~~ (LogReg vs LightGBM 2종이면 서사 성립)
6. ~~SHAP 워터폴 디자인~~ (막대 차트로 충분)

**절대 버리지 않는 것**
- 합성 데이터 명시 (숨기면 발각 시 타격이 훨씬 크다)
- 단조 제약 서사 (이게 없으면 평범한 분류 모델 프로젝트가 된다)
- 조정 가능/불가능 변수 분리 (이게 없으면 최적화가 헛소리가 된다)
- `make demo` 가 뜨는 것
