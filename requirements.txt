# YEDA 의존성 — 버전 고정
# ---------------------------------------------------------------------------
# 5명이 서로 다른 노트북에서 작업한다. 버전이 흔들리면 "내 컴퓨터에서는 되는데"가
# 발생하고, 그 디버깅에 쓰는 시간이 24시간 중 가장 아까운 시간이 된다.
# 버전을 올리려면 팀 전체에 공지하고 통합 담당자(E)가 확인한 뒤 반영한다.
#
# 설치:  make setup   (또는  pip install -r requirements.txt)
# Python 3.11 이상 필요
# ---------------------------------------------------------------------------

# --- 데이터 ---
numpy==2.4.6
pandas==3.0.5
PyYAML==6.0.3

# --- 모델 ---
scikit-learn==1.9.0
lightgbm==4.7.0
joblib==1.5.3

# --- 설명 ---
shap==0.51.0

# --- 최적화 ---
scipy==1.17.1
# optuna 는 선택 사항이다. 없으면 랜덤 서치로 자동 폴백하므로 데모는 그대로 돌아간다.
# 시간이 남으면(M5 이후) 아래 주석을 풀 것.
# optuna==4.6.0

# --- UI ---
streamlit==1.61.1
matplotlib==3.11.1

# --- API (3-Tier 백엔드) ---
fastapi==0.116.0
uvicorn[standard]==0.34.3
httpx==0.28.1
pymysql==1.1.1
sqlalchemy==2.0.41

# --- 테스트 ---
pytest==9.1.1

# --- 알림 ---
# smtplib 은 표준 라이브러리라 설치가 필요 없다.
# .env 로 SMTP 자격증명을 주입할 때만 아래를 켠다.
# python-dotenv==1.1.1
