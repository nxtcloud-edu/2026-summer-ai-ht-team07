[pytest]
# src 레이아웃을 쓰므로 PYTHONPATH 를 pytest 가 직접 잡아준다.
# 이게 없으면 각자 노트북에서 "import yeda 안 됨" 으로 시간을 버린다.
pythonpath = src
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
